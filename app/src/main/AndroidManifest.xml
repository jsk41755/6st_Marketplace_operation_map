<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ff_simbol"
        android:label="@string/app_ff_name"
        android:roundIcon="@mipmap/ic_ff_simbol_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testtest">
        <activity android:name=".Zone3"></activity>
        <activity android:name=".Zone2" />
        <activity android:name=".Zone1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>